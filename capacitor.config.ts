import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'festival-friends',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
